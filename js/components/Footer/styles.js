import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    textAlign: 'left',
    width: '90%',
    marginBottom: 40,
    marginTop: 20
  }
});

export default styles;
